services:
  - type: web
    name: huvoice-ai-app
    runtime: python
    pythonVersion: 3.11
    
    # Build: Install dependencies from root requirements.txt
    buildCommand: pip install -r requirements.txt

    # Start: Run unified FastAPI server (NOT Flask app.py)
    startCommand: python server.py

    # Port configuration
    port: 5000

    # Environment variables
    envVars:
      # Port configuration
      - key: PORT
        value: "5000"
      
      # Critical: OpenRouter API Key (set in Render dashboard)
      - key: OPENROUTER_API_KEY
        sync: false
      
      # API Configuration
      - key: OPENROUTER_MODEL
        value: openai/gpt-4o-mini
      
      - key: CORS_ORIGINS
        value: "*"
      
      # Environment
      - key: DEBUG
        value: "false"
      
      - key: HOST
        value: 0.0.0.0